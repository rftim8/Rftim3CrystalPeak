<UserControl x:Class="Rftim3WPFUCL.RftDataGrids.RftDataGrid_0"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Rftim3WPFUCL.RftDataGrids" 
             mc:Ignorable="d">

    <DockPanel Background="{DynamicResource RftBrushBackgroundWindow}">
        <Label DockPanel.Dock="Top"
               Height="50"
               FontSize="25"
               HorizontalAlignment="Center"
               VerticalAlignment="Center"
               Foreground="{DynamicResource RftBrushForegroundWindow}">
            Data Grid
        </Label>

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="100"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Grid Grid.Column="0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="50"/>
                    <RowDefinition Height="50"/>
                    <RowDefinition Height="50"/>
                    <RowDefinition Height="50"/>
                    <RowDefinition Height="50"/>
                </Grid.RowDefinitions>

                <Button Grid.Row="0"
                        Content="Create"
                        Command=""/>
                <Button Grid.Row="1"
                        Content="Delete"
                        Command=""/>
                <Button Grid.Row="2"
                        Content="GetAll"/>
                <Button Grid.Row="3"
                        Content="Get"
                        Command=""/>
                <Button Grid.Row="4"
                        Content="Update"
                        Command=""/>
            </Grid>

            <DataGrid Grid.Column="1"
                      ItemsSource="{Binding Persons}">
            </DataGrid>
        </Grid>
    </DockPanel>
</UserControl>
