<UserControl x:Class="Rftim3WPFUCL.RftListBoxes.RftListBox_0"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Rftim3WPFUCL.RftListBoxes"
             mc:Ignorable="d">

    <DockPanel Background="{DynamicResource RftBrushBackgroundWindow}">
        <Label DockPanel.Dock="Top"
               Height="50"
               FontSize="25"
               HorizontalAlignment="Center"
               VerticalAlignment="Center"
               Foreground="{DynamicResource RftBrushForegroundWindow}">
            List Box
        </Label>
        <Grid>
            <Grid.Resources>
                <AlternationConverter x:Key="BackgroundConverter">
                    <SolidColorBrush>Blue</SolidColorBrush>
                    <SolidColorBrush>red</SolidColorBrush>
                </AlternationConverter>

                <AlternationConverter x:Key="FontStyleConverter">
                    <FontStyle >Italic</FontStyle>
                    <FontStyle >Normal</FontStyle>
                </AlternationConverter>

                <Style x:Key="alternatingWithBinding" TargetType="{x:Type ListBoxItem}">
                    <Setter Property="Background"
                        Value="{Binding RelativeSource={RelativeSource Self},
                    Path=(ItemsControl.AlternationIndex),
                    Converter={StaticResource BackgroundConverter}}"/>

                    <Setter Property="FontStyle"
                        Value="{Binding RelativeSource={RelativeSource Self},
                    Path=(ItemsControl.AlternationIndex),
                    Converter={StaticResource FontStyleConverter}}"/>
                </Style>

            </Grid.Resources>

            <ListBox Background="{DynamicResource RftBrushBackgroundWindow}"
                     AlternationCount="2"
                     ItemContainerStyle="{StaticResource alternatingWithBinding}">
                <ListBoxItem Width="500"
                         Height="25"
                         Content="werwedsgrte"></ListBoxItem>
                <ListBoxItem Width="500"
                         Height="25"
                         Content="wwwwwwwwwwwwwww"></ListBoxItem>
                <ListBoxItem Width="500"
                         Height="25"
                         Content="wwwwwwwwwwwwwww"></ListBoxItem>
                <ListBoxItem Width="500"
                         Height="25"
                         Content="wwwwwwwwwwwwwww"></ListBoxItem>
                <ListBoxItem Width="500"
                         Height="25"
                         Content="wwwwwwwwwwwwwww"></ListBoxItem>
            </ListBox>
        </Grid>
    </DockPanel>
</UserControl>
