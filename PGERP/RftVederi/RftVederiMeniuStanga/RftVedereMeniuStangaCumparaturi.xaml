<UserControl x:Class="PGERP.RftVederi.RftVedereMeniuStangaCumparaturi"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PGERP.RftVederi"
             xmlns:rftComponentePersonalizate="clr-namespace:PGERPComponentePersonalizate;assembly=PGERPComponentePersonalizate"
             mc:Ignorable="d" 
             d:DesignWidth="250">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="50"/>
            <RowDefinition Height="50"/>
            <RowDefinition Height="50"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="200"/>
                <ColumnDefinition Width="50"/>
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Column="0"
                       Style="{StaticResource RftStilComponentaMeniuStanga}"
                       Text="Alimente">
            </TextBlock>

            <rftComponentePersonalizate:RftComponentaPersonalizataButonRadioImagine Grid.Column="1"
                                                                                    GroupName="One"
                                                                                    ImageSource="{StaticResource RftSursaImagineAlimente}"
                                                                                    Command="{Binding RftComandaAccesareVedereContinutAlimente}"/>
        </Grid>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="200"/>
                <ColumnDefinition Width="50"/>
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Column="0"
                       Style="{StaticResource RftStilComponentaMeniuStanga}"
                       Text="Imbracaminte">
            </TextBlock>

            <rftComponentePersonalizate:RftComponentaPersonalizataButonRadioImagine Grid.Column="1"
                                                                                    GroupName="One"
                                                                                    ImageSource="{StaticResource RftSursaImagineImbracaminte}"
                                                                                    Command="{Binding RftComandaAccesareVedereContinutImbracaminte}"/>
        </Grid>
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="200"/>
                <ColumnDefinition Width="50"/>
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Column="0"
                       Style="{StaticResource RftStilComponentaMeniuStanga}"
                       Text="Electronice">
            </TextBlock>

            <rftComponentePersonalizate:RftComponentaPersonalizataButonRadioImagine Grid.Column="1"
                                                                                    GroupName="One"
                                                                                    ImageSource="{StaticResource RftSursaImagineElectronice}"
                                                                                    Command="{Binding RftComandaAccesareVedereContinutElectronice}"/>
        </Grid>
    </Grid>
</UserControl>
